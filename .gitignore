.gradle/
build/
out/
.idea/
*.iml
.DS_Store

# Reference implementations cloned locally for porting/comparison - gosrt (Go, MIT)
# as the structural reference, libsrt (MPL-2.0) as the interop target.
# Not ours to push upstream - keep out of this repo's history.
/references/

# Working notes kept out of the repository. Useful in a checkout, but they are
# narratives of decisions that the javadoc, README and commit messages already
# record - and while STATUS.md was tracked it went stale five times, twice in
# ways that cost real time. DESIGN.md currently lives a directory up, outside
# this repository, so that rule matches nothing today and is only a guard.
CLAUDE.md
DESIGN.md
STATUS.md
